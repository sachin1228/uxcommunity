import { DashboardContentLoader } from "../../ContentLoader";

/** Thread detail loading boundary — centered spinner without secondary rails. */
export default function ThreadDetailLoading() {
  return <DashboardContentLoader />;
}
