import { DashboardContentLoader } from "../../ContentLoader";

/** Event detail loading boundary — centered spinner without secondary rails. */
export default function EventDetailLoading() {
  return <DashboardContentLoader />;
}
