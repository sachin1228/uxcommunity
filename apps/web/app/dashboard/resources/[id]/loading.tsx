import { DashboardContentLoader } from "../../ContentLoader";

/** Resource detail loading boundary — centered spinner without secondary rails. */
export default function ResourceDetailLoading() {
  return <DashboardContentLoader />;
}
