/**
 * Shared video-pipeline types — live in `@uxcommunity/shared`
 * (packages/shared/src/video/video-types.ts) so the web app and the
 * server-side transcoder service share the exact same contracts.
 * Re-exported here for the app's existing import surface.
 */
export * from "@uxcommunity/shared";