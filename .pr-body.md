## What

Replaces the threads comment section with the new design (screenshot-accurate), scoped to **threads only** — resources / showcase / events keep their current composer.

### New design
- **Surface card** with a "Comments" heading
- **Inline composer** — 52px rounded field: emoji picker on the left, auto-growing input, blue **Send** pill; grows with content and shows a focus halo
- **Toolbar row** — comment count (message icon) on the left, **Most recent / Most popular** sort dropdown on the right
- **Timeline comments** — small avatars, `name • 5m ago` headers, dot + dashed connector on the left, vertical ⋮ options menu (delete, for owners)
- **Reactions** — smiley+ pill opens an emoji picker (👍 ❤️ 🎉 💡 👏); active reactions render as count pills and highlight when yours
- **Replies** — "View / Hide N replies" toggle; the reply composer gets a Cancel pill

### Backend (minimal, threads-only)
- New `thread_comment_reactions` table (unique per user+emoji, RLS) — `supabase/migrations/20260908120000_thread_comment_reactions.sql`
- `POST …/comments/:id/reactions` toggle endpoint returning authoritative grouped counts
- Comments `GET`/`POST` now attach grouped `reactions` to each comment
- Shared `CommentComposer` gains an opt-in `variant="inline"`; the default variant is untouched

## Notes
- ⚠️ Requires applying the migration before reactions are usable — the UI degrades gracefully (comments render, reaction button hidden on error).
- The reactions table is intentionally threads-only; other sections can adopt the same pattern later without widening this PR.
- Verified: typecheck (no new errors), ESLint clean, 21 unit tests pass, production build passes.

🤖 Generated with Codebuff
