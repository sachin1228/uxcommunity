Community switching performance is now good, but there is one remaining issue:

When I do a full browser refresh while viewing a community chat, the initial chat takes around 1–2 seconds to appear.

The module-level cache is obviously empty after a full refresh, so a cold fetch is required. However, I want to understand whether the remaining 1–2 second delay can be reduced before adding persistent browser caching.

Please investigate the cold-load path for:

/dashboard/communities/[communityId]

Measure the actual timing separately for:

1. GET /api/communities
2. GET /api/communities/[id]
3. GET /api/communities/[id]/messages
4. Supabase query execution time inside each endpoint
5. Network/server latency
6. React rendering/hydration time

Check whether community metadata and messages are currently fetched sequentially or in parallel on initial load.

The desired cold-load flow should be:

Page refresh
    ↓
Community ID available from URL
    ↓
Immediately start in parallel:
    ├── Fetch community metadata
    ├── Fetch latest messages
    └── Fetch sidebar communities
    ↓
Render each section independently as soon as its data arrives

Do not make the chat messages wait for `/api/communities` or `/api/communities/[id]` if the messages endpoint can be called immediately using the community ID from the URL.

Also investigate:

- Whether authentication/session lookup is adding significant latency to every API request.
- Whether each of the 3 API endpoints independently performs the same Supabase auth/membership checks.
- Whether the Supabase client is being initialized inefficiently.
- Whether the new `(community_id, created_at DESC)` index is actually being used by the messages query.
- Whether sender/profile/avatar hydration is still causing slow message queries.
- Whether server-side rendering or Next.js server components could provide the initial community/messages data and hydrate the client cache, avoiding the client-side loading waterfall.
- Whether `/api/communities/[id]` is actually necessary for initial chat rendering if the selected community metadata already exists in `/api/communities`.

Please measure first. Do not blindly add localStorage or IndexedDB.

Report exact timings and identify the bottleneck.

If the backend requests themselves are fast but the unavoidable cold network round-trip is causing the delay, then propose the best approach for persistent cache or server-side initial data.

My priority is:
1. Reduce actual backend/database latency first.
2. Eliminate sequential request waterfalls.
3. Render messages independently from sidebar/metadata.
4. Only then consider persistent caching for instant refresh.

Do not make changes until you have traced and explained the cold-load bottleneck to me.