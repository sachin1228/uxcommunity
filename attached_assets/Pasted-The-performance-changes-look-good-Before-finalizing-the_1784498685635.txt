The performance changes look good. Before finalizing the PR, please verify the actual network behavior in the browser rather than relying only on theoretical request counts.

Specifically test:

1. Fresh page load
2. First click on Community A
3. Click Community B
4. Return to Community A
5. Rapidly switch A → B → C → A → B → C
6. Receive a Realtime message in the currently selected community
7. Receive a Realtime message in a community that is not currently selected
8. Send a message yourself and verify the Realtime event does not create a duplicate
9. Hover over multiple communities quickly and verify the 200ms debounce prevents excessive prefetch requests

For each scenario, report the exact API requests observed.

Also verify these potential issues:

- On revisiting a cached community, confirm whether both metadata AND messages are being background-refetched. If yes, the claim of "0–1 API calls" is incorrect. Avoid refreshing metadata on every revisit unless it is stale. Add an appropriate TTL/stale-time if necessary.

- Ensure the module-level Map cache cannot grow indefinitely. Consider a reasonable TTL or maximum cache size if necessary, but keep the implementation simple.

- Ensure concurrent requests are deduplicated. If hover prefetch starts a messages request and the user clicks before it finishes, the click should reuse the in-flight request rather than start another identical request.

- Ensure stale responses cannot overwrite the currently selected community when users switch rapidly.

- Ensure the `?after=ISO` incremental message query is correctly indexed in Supabase/Postgres. The query should ideally have an index appropriate for community_id + created_at.

- Ensure Realtime messages for non-selected communities update the correct cache and sidebar last-message state without causing unnecessary API refetches.

- Verify that browser back/forward navigation works correctly with cached communities.

- Check for race conditions when switching communities rapidly.

Then run:
- TypeScript check
- lint
- production build
- relevant tests

Report:
1. Actual API request counts before vs after for each scenario
2. Actual response-time improvement
3. Any remaining bottlenecks
4. Any database indexes added or recommended

If everything is correct, update the existing PR with any necessary fixes. Do not merge it.